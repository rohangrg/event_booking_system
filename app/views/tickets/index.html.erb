<p style="color: green"><%= notice %></p>

<h1>Tickets</h1>

<div id="tickets">
  <% @tickets.each do |ticket| %>
    <div class="mb-3 p-3 border">
      <%= render ticket %>
    </div>
  <% end %>
</div>